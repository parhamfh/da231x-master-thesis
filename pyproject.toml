[project]
name = "sonata"
version = "0.1.0"
description = "Music rearrangement for your ears."
authors = [
    { name = "parhamfh", email = "parhamfh@example.com" },
]
requires-python = ">=3.10"
license = { text = "Apache-2.0" } # Assuming MIT based on LICENSE.md

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 100
